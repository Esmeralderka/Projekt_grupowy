<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekt_grupowy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projekt_grupowy.NoActionBar">
        <activity android:name=".DocumentProperties" />
        <activity android:name=".Export" />
        <activity
            android:name=".activity_documents"
            android:theme="@style/Theme.Projekt_grupowy.NoActionBar" />

        <activity
            android:name=".AddDocumentActivity"
            android:theme="@style/Theme.Projekt_grupowy.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Projekt_grupowy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_base" />
        <activity android:name=".Register" />

    </application>

</manifest>